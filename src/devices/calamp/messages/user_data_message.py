
class UserDataMessage (IterPacket):
    
    def __init__ (self, packet, packet_offset):
        """
        Create a new UserDataMessage instance.

        @param packet           The packet.
        @param packet_offset    The packet offset.

        """
        # IterPacket.__init__(self, packet, packet_offset, "USER_DATA_MESSAGE")
		pass